/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/workshop-changsha-2025',
  distDir: 'docs', // Optional: change the default build directory
  reactStrictMode: true,
}

module.exports = nextConfig
